
general {
    lock_cmd = pidof hyprlock || hyprlock       
}
listener {
    timeout = 300 # 5min
    on-timeout = loginctl lock-session
}
listener {
    timeout = 600 # 10min
    on-timeout = systemctl suspend # or hibernate
}

