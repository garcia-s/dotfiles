
### I want my shell to have the following

1. A top bar that contains the following characteristics

- A button that opens a application menu that is integrated with the shell
- A button to open a menu to shutdown, reset, sleep, hibernate the pc.
- A clock in the middle that when clicked displays a calendar with the current  date properly accented
- A section showing cpu overall usage. When clicked should show a breakdown of per CPU usage.
- A section showing memory usage. When clicket it could show the most used processes in memory.
- A section for gpu usage. It should display both memory and processing usage as a percentage.

I want this top bar to be separated from the edges by like 10px

2. All the panels above shoud be able to be open via CLI since they have to be integrated into hyprland.

ne
